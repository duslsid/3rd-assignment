/* 이 파일을 js/config.js 로 복사한 뒤 값을 채워 주세요.
   js/config.js 는 .gitignore 로 저장소에 올라가지 않으며,
   배포 시에는 GitHub Actions 가 저장소 Secrets 로 자동 생성합니다. */
window.SUPABASE_CONFIG = {
  url: 'https://<프로젝트-ref>.supabase.co',
  key: '<supabase-publishable-key>',
};
